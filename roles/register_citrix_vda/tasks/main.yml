---
- name: Register VDA with enrollment token
  become: true
  ansible.builtin.command:
    cmd: /opt/Citrix/VDA/bin/VdaEnrollmentTool -EnrollmentToken:{{ register_citrix_vda_enrollment_token }} -Restart
  changed_when: true
